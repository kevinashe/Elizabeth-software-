apiVersion: v1
kind: Secret
metadata:
  name: {{ include "my-service.fullname" . }}-secret
type: Opaque
stringData:
  # Fill in via helm --set or sealed-secrets
  APP_SECRET: {{ .Values.secrets.appSecret | default "" }}
